<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity" >


    <TextView
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="@string/nfc_text"
        android:id="@+id/nfc_text"
        android:layout_above="@+id/write_text_button"
        android:textSize="20dp"
        android:textIsSelectable="false"
        android:layout_alignParentTop="true" />

    <Button
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="@string/write_aar_button"
        android:id="@+id/write_aar_button"
        android:layout_gravity="center"
        android:layout_centerVertical="true"
        android:layout_alignRight="@+id/write_text_button"
        android:layout_alignLeft="@+id/write_text_button" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/write_text_button"
        android:id="@+id/write_text_button"
        android:layout_gravity="center"
        android:layout_above="@+id/write_url_button"
        android:layout_centerHorizontal="true" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/write_url_button"
        android:id="@+id/write_url_button"
        android:layout_gravity="center"
        android:layout_above="@+id/write_aar_button"
        android:layout_alignLeft="@+id/write_aar_button"
        android:layout_alignRight="@+id/write_aar_button" />

</RelativeLayout>